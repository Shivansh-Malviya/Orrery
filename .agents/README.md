# `.agents/` — Cross-Platform Shared Resources

**Rules** (Antigravity): `.agents/rules/` is agy's native path — no deploy needed.
**Skills**: Shared skill definitions — add `.agents/skills` to platform skill path.
**Workflows**: Shared multi-step agent procedures.

Platform-specific files stay in their standard locations:
- `.github/copilot-instructions.md` (VS Code)
- `.github/hooks/` (VS Code, OpenCode)
- `.cursor/rules/` (Cursor)
- Root `AGENTS.md` (all platforms)

See `AGENTS.md` in each subdirectory for details.
