# lib

**Shared reusable code.**

Shared code libraries.

## Structure
- `python-utils/` — Python utility modules

## Policy
Libraries are protected by `policy/OUTPUT_PATH_POLICY.md` — read-only unless explicitly authorized.
