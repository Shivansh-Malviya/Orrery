> **Historical note:** This `BROS_Archive/` directory was migrated from
> `F:\BROS_SYSTEM\archive\` on 2026-06-07 as part of the Orrery bootstrap.
> The artifacts in `planning-chain/` and `legacy_agents_md/` are
> reference-only historical records. Path references inside archived files
> are from the original source (`F:\BROS_SYSTEM\`) and are preserved as-is.
> Do not treat archived content as active policy or live configuration.
> For active path-safety rules, see `F:\Orrery\policy\OUTPUT_PATH_POLICY.md`.

# BROS_Archive

Historical artifacts from the bootstrap planning phase and unprocessed advisory imports.

## Contents

| Path | Role |
|------|------|
| `planning-chain/` | B0-B4 planning artifacts (formerly BROS_SYSTEM\bootstrap\) — import manifests, claim ledgers, blocker registers, hash manifests, compliance matrices. **Historical; do not execute.** |
| `legacy_agents_md/` | Pre-Orrery AGENTS_*.md files from BROS_SYSTEM legacy bootstrap. **Historical reference only.** |
| `planning-chain/governance/OUTPUT_PATH_POLICY.md` | Pre-Orrery BROS_SYSTEM version of the output-path policy. **Superseded by `F:\Orrery\policy\OUTPUT_PATH_POLICY.md`.** |

## Rules

- `planning-chain/` is complete and frozen. Reference-only.
- `legacy_agents_md/` is historical. Reference-only.
- The pre-Orrery OUTPUT_PATH_POLICY in `planning-chain/governance/` is superseded by the active version at `F:\Orrery\policy\OUTPUT_PATH_POLICY.md`.
