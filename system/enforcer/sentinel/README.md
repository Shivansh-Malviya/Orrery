# enforcer/sentinel/ — Continuous Monitoring

Sentinel scripts that run periodically (via scheduled hooks) to detect
policy drift and stale state.

## Planned

- `health-check.ps1` — Periodic system health validation
- `sentinel.ps1` — Continuous monitoring daemon
