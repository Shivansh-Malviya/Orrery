# scheduler/conditions/ — Trigger Conditions

Each file defines a trigger condition that the scheduler evaluates.

## Format

```yaml
condition: <name>
trigger: <event pattern>
action: <skill or workflow to dispatch>
priority: <high|medium|low>
```
