# system/scheduler/ — Skill Dispatching

Evaluates triggers and dispatches skills/workflows based on session context.

## Structure

- `conditions/` — Trigger condition definitions

## Planned

- `scheduler.ps1` — Evaluates conditions and dispatches
- `trigger-registry.yaml` — Maps conditions to skills/workflows
