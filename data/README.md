# data

**Structured data.** Raw data is protected by policy.

Raw data files (large or generated).

## Structure
- `raw/` — Raw data files (large or generated)

## Policy
Raw data is protected by `policy/OUTPUT_PATH_POLICY.md` — read-only access unless explicitly authorized.
